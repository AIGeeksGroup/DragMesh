# DragMesh dependencies 

# Core (runtime)
numpy
tqdm
lmdb
pyyaml
trimesh

# Optional: visualization / export / interactive UI (pinned to known-good versions)
pyrender==0.1.45
imageio==2.37.2
imageio-ffmpeg==0.6.0
pygltflib==1.16.5
viser==1.0.16
requests>=2.28

# Optional: training logging (pinned to known-good versions)
tensorboard==2.20.0
wandb==0.23.1


